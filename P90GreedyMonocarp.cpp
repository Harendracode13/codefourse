#include<bits/stdc++.h>
using namespace std;

int main()
{
	int t;
	cin>>t;

	while(t--)
	{
		int n,k;
		cin>>n>>k;
		int ans=0;

       vector<int> v(n);
	   int mixi=v[0];
		for(int i=0;i<n;i++)
		{
			cin>>v[i];
		}
		sort(v.begin(),v.end());

		for(int i=0;i<n;i++)
		{
			if(k>v[n-i-1])
		}

	}
}