#include<bits/stdc++.h>
using namespace std;

int main()
{
    long long t;
    cin>>t;
    long long n,q;
    cin>>n>>q;

    vector<long long> a(n),b(n);
    for(long long i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(long long i=0;i<n;i++)
    {
        cin>>b[i];
    }
    
}
